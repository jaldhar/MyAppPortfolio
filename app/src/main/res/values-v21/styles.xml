<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="myButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:backgroundTintMode">add</item>
        <item name="android:backgroundTint">@color/colorAccent</item>
    </style>

    <style name="leftButton" parent="myButton">
        <item name="android:layout_marginRight">@dimen/cell_right_margin</item>
    </style>

    <style name="rightButton" parent="myButton">
        <item name="android:layout_marginLeft">@dimen/cell_left_margin</item>
    </style>

</resources>
